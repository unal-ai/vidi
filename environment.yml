name: vidi
channels:
  - conda-forge
  - nvidia
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  # System dependencies
  - ffmpeg
  - git
  # Core packages via conda
  - numpy
  - pandas
  - matplotlib
  - pillow
  - scipy
  # Install remaining via pip
  - pip:
    - torch==2.3.1
    - torchvision==0.18.1
    - torchaudio==2.3.1
    - transformers==4.44.2
    - sentencepiece==0.1.99
    - shortuuid
    - accelerate==0.33.0
    - peft
    - bitsandbytes
    - pydantic
    - scikit-learn==1.2.2
    - requests
    - httpx==0.22.0
    - decord
    - uvicorn
    - fastapi
    - einops==0.6.1
    - einops-exts==0.0.4
    - timm==0.6.13
    - deepspeed==0.14.5
    - ninja
    - wandb
